apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app-green
  labels:
    app: demo-app
    version: green
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-app
      version: green
  template:
    metadata:
      labels:
        app: demo-app
        version: green
    spec:
      containers:
        - name: demo-app
          image: omkar8284/jenkins-demo:latest
          ports:
            - containerPort: 80
          imagePullPolicy: Always

