apiVersion: v1
kind: Service
metadata:
  name: demo-app-service
spec:
  selector:
    app: demo-app
    version: blue   # change to green to switch traffic
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP

